#include <stdio.h>
#include <stdbool.h>
#define NELEMS(x)  (sizeof(x) / sizeof((x)[0]))

int main() {
    int test[] = {2,3,1,0,2,5,3};

    printf("%s: %d\n", is ? "Yes" : "No", duplication);

   return 0;
}
