#pragma once
#include <vector>

int videoStitching(const std::vector<std::vector<int>> &clips, int T);

int findInMountainArray(int target, const std::vector<int> &mountainArr);

bool uniqueOccurrences(const std::vector<int> &arr);
